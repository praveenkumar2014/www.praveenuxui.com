/* ajax-form.js — DISABLED to prevent double-submit conflict with animations.js initFormSubmit().
 * The Fetch-based handler in animations.js is the single source of truth for #contact-form.
 * Keeping this file intact for reference; remove this guard to re-enable jQuery AJAX fallback.
 */
// $(function () { ... }); // deactivated
